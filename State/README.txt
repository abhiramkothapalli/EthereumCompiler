Files to generate solidity code from state language

First install antlr runtime:

sudo pip3 install antlr4-python3-runtime

Then run the converter:

./convert file.state

A full grammar describing the rules of the state language is available in ../ANTLR/grammars/state.g4
